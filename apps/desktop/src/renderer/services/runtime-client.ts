/**
 * Neutral runtime client facade. The current implementation is backed by the
 * OpenCode-compatible runtime client, but renderer consumers should depend on
 * this surface rather than project-specific naming.
 */
export * from "./project-runtime-client"
