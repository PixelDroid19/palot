/**
 * Neutral runtime SDK facade. The current implementation is backed by the
 * OpenCode-compatible runtime SDK, but renderer consumers should depend on
 * this surface rather than provider-specific naming.
 */
export * from "./project-runtime-sdk"
