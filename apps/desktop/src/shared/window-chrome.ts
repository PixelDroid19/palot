/**
 * Window chrome constants shared between main (BrowserWindow) and renderer (layout).
 */

/** Height of the custom app bar — must match `titleBarOverlay.height` on Linux/Windows. */
export const APP_BAR_HEIGHT_PX = 46

/** Default title bar overlay color (dark theme background). */
export const TITLE_BAR_OVERLAY_COLOR = "#181818"
