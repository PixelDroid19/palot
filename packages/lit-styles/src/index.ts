export { palotLitScss } from "./vite-plugin-palot-lit-scss"
/**
 * Style generation (SCSS->css.js + .d.ts for Lit components):
 *   bun run build:styles   # from repo root (turbo, recommended)
 *   cd packages/lit-styles && bun run build:styles
 * Wires via turbo task + root script (foundational integration). See docs/IMPORT-ARCHITECTURE.md for monorepo platform pkgs.
 */
