# @palot/agent-adapter-claude-code

**Phase 2 / placeholder** — Claude Code provider adapter skeleton.

Implements the `AgentProviderAdapter` contract from `@palot/core` at the type level so wiring, tests, and future UI surfaces can prepare for multi-provider support without depending on a real implementation.

See:

- `src/index.ts` (intentionally minimal + safe no-op iterables)
- `test/skeleton.test.ts`
- `roadmap/core-agent-platform.md`, `roadmap/recommendations.md`
- `@palot/agent-adapter-opencode` for the reference impl

Do not use in production paths yet. Will be filled when surface migration + Codex/Claude work begins.
