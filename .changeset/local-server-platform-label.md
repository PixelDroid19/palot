---
"@palot/desktop": patch
---

Fix local server label to match the host OS on Linux and Windows, remove legacy unsupported `local-grok` server entries from settings on startup, and drop orphaned credentials for removed servers.
